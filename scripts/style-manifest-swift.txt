# Exemption manifest for the code-style rollout (#95).
# Seeded 2026-08-12 with every Sources/OCCTSwiftViewport/*.swift file that existed at
# that point. A file is removed from this list once it's brought into full
# swift-format/swiftlint compliance; touching a listed file without removing it
# fails scripts/check-style-manifest.py. See okf/policies/code-style.md.
#
# The list is now EMPTY, and deliberately so: the rollout finished on 2026-08-19 with
# #112 (2 entries) and #113 (the remaining 46). Every file under Sources/OCCTSwiftViewport
# is checked unconditionally by the code-style workflow. Keep it empty. Nothing is
# grandfathered back on: new code complies from creation, which is what the manifest
# only ever shrinking was for.
